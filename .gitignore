# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/
build/
next-env.d.ts

# Production
dist/

# Environment & secrets
.env
.env.*
!.env.example

# Vercel
.vercel

# Brainstorming visual companion (local only)
.superpowers/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
*.swp

# Playwright MCP artifacts & smoke screenshots
.playwright-mcp/
phase*.png

# impeccable live-mode local config/journal (dev tool, not project source)
.impeccable/

# Claude Code local settings (machine-local; may hold personal permission grants).
# The impeccable PostToolUse hook lives here and keeps working locally + via the
# OneDrive backup; it is intentionally not version-controlled.
.claude/settings.local.json

# Source image drop folder (originals; optimized derivatives live in public/images/).
# Root-anchored + case note: Git is case-insensitive on Windows, so an unanchored
# "Images/" would also match public/images/ — the leading slash keeps it root-only.
/Images/

# TypeScript incremental build cache
tsconfig.tsbuildinfo
*.tsbuildinfo

# Newsletter preview output
.preview/

# Browser "Save page as" debris (*_files/ next to a saved .html) must never reach public/.
public/images/*_files/
